// import * as xxxActions from '../actions/xxxActions'

const reducer = (state: any = [], action: any) => {
  switch (action.type) {
    default: {
      return state
    }
  }
}

export default reducer
